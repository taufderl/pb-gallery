.col-md-offset-4.col-md-3
  = render :partial => "new"
  .btn-group<
    = link_to t("links.sign up"), new_registration_path(resource_name), class: 'btn btn-default'
    = link_to t("links.forgot_password"), new_password_path(resource_name), class: 'btn btn-default'

